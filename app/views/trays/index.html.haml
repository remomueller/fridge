%h1
  .float-right= link_to "New Tray", new_tray_path, class: "btn btn-sm btn-primary"
  Trays

%table.table.table-borderless.table-striped
  %thead
    %tr
      %th Name
      %th Slug
      %th{ colspan: 3 }
  %tbody
    - @trays.each do |tray|
      %tr
        %td= link_to tray.name, tray
        %td= tray.slug
        %td= link_to "Show", tray
        %td= link_to "Edit", edit_tray_path(tray)
        %td= link_to "Destroy", tray, method: :delete, data: { confirm: "Are you sure?" }
